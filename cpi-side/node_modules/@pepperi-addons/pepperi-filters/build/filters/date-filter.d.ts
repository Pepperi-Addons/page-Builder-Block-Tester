import Filter from './filter';
import { DateOperation } from '../json-filter';
export declare class DateFilter extends Filter {
    private operation;
    private filterValues;
    constructor(apiName: string, operation: DateOperation, filterValues: string[]);
    apply(value: any): boolean;
    toSQLWhereClause(): string;
    apiDateValue(val: string): string;
}
