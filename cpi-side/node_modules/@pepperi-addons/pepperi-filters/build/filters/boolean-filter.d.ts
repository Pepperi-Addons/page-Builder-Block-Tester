import Filter from './filter';
export declare class BooleanFilter extends Filter {
    private filterValue;
    constructor(apiName: string, filterValue: boolean);
    apply(value: any): boolean;
    toSQLWhereClause(): string;
}
