import Filter from './filter';
import { BasicOperations } from '../json-filter';
export declare class GuidFilter extends Filter {
    private operation;
    private filterValue;
    constructor(apiName: string, operation: BasicOperations, filterValue: string);
    apply(value: any): boolean;
    standardize(guid: any): string;
    toSQLWhereClause(): string;
}
