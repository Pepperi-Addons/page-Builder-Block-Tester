import Filter from './filter';
import { NumberOperation } from '../json-filter';
export declare class NumberFilter extends Filter {
    private operation;
    private filterValues;
    constructor(apiName: string, operation: NumberOperation, filterValues?: number[]);
    apply(value: any): boolean;
    toSQLWhereClause(): string;
}
