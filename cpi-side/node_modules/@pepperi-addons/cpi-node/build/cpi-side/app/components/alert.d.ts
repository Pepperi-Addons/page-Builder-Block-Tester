export declare class Alert {
    title: string;
    message: string;
    actions: AlertAction[];
    constructor(title: string, message: string, actions: AlertAction[]);
    show(): void;
    getClientAction(action: any): {
        key: any;
        title: any;
    };
}
export declare class AlertAction {
    title: string;
    handler: (action: AlertAction) => any;
    id: string;
    constructor(title: string, handler: (action: AlertAction) => any);
    onSelect(): void;
}
