import { Item, Transaction, TransactionLine } from './data-object';
export declare class TransactionScope {
    /**
     * Create a TransactionScope object for a transaction
     * @param transaction The transaction
     */
    static Get(transaction: Transaction): Promise<TransactionScope>;
    getItems(): Promise<TransactionLine[]>;
    getItem(item: Item): Promise<TransactionLine | undefined>;
}
