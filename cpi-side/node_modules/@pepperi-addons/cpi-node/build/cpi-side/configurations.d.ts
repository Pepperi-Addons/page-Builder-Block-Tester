import { Configuration, CPISideFiles } from '../shared/configurations';
export declare class ConfigurationLoader {
    files: {
        [key: string]: CPISideFiles;
    };
    load(): Promise<void>;
    getConfigurations(): Promise<Configuration[]>;
    getFiles(): Promise<CPISideFiles[]>;
    getFilePath(): Promise<CPISideFiles[]>;
    loadConfiguration(configuration: Configuration): Promise<void>;
}
