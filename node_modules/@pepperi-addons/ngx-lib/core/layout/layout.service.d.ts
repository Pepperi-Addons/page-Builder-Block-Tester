import { Observable } from 'rxjs';
import { TranslateService } from '@ngx-translate/core';
export declare type PepOrientationType = 'landscape' | 'portrait';
export declare enum PepScreenSizeType {
    XL = 0,
    LG = 1,
    MD = 2,
    SM = 3,
    XS = 4
}
export declare class PepLayoutService {
    private translate;
    private resizeSubject;
    private deviceHasMouseSubject;
    get onResize$(): Observable<PepScreenSizeType>;
    get onMouseOver$(): Observable<boolean>;
    constructor(translate?: TranslateService);
    private documentMouseoverListener;
    /**
     * Set the current screen size.
     * @param size The size to change to.
     */
    onResize(size: PepScreenSizeType): void;
    getDeviceHasMouse(): boolean;
    getOrintation(): PepOrientationType;
    getScreenWidth(): number;
    getScreenHeight(): number;
    getScreenHeightPx(): number;
    getScreenWidthPx(): number;
    getScrollbarWidth(): number;
    getCurrentLanguage(): string;
    isRtl(): boolean;
}
