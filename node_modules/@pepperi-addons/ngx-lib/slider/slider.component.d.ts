import { EventEmitter, Renderer2, ElementRef, OnInit } from '@angular/core';
import { PepLayoutService, PepHorizontalAlignment } from '@pepperi-addons/ngx-lib';
/**
 * This is a slider component that support pepperi theme
 * style & state & sizes
 *
 * @export
 * @class PepSliderComponent
 * @implements {OnDestroy}
 */
export declare class PepSliderComponent implements OnInit {
    private renderer;
    private element;
    private pepLayoutService;
    label: string;
    disabled: boolean;
    hint: string;
    background: string;
    step: number;
    minValue: number;
    maxValue: number;
    private _value;
    set value(value: string);
    get value(): string;
    valueChange: EventEmitter<string>;
    xAlignment: PepHorizontalAlignment;
    constructor(renderer: Renderer2, element: ElementRef, pepLayoutService: PepLayoutService);
    ngOnInit(): void;
    onValueChange(event: any): void;
}
