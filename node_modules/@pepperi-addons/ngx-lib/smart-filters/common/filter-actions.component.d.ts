import { EventEmitter } from '@angular/core';
import { FormGroup } from '@angular/forms';
export declare class PepFilterActionsComponent {
    form: FormGroup;
    clearClick: EventEmitter<void>;
    applyClick: EventEmitter<boolean>;
    clear(): void;
    apply(): void;
}
