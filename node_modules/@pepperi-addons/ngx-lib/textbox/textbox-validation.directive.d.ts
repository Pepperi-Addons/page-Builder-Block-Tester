import { ElementRef, OnInit } from '@angular/core';
import { PepTextboxFieldType, PepValidatorService } from '@pepperi-addons/ngx-lib';
export declare class PepTextboxValidationDirective implements OnInit {
    private hostElement;
    private validatorService;
    type: PepTextboxFieldType;
    previousValue: string;
    constructor(hostElement: ElementRef, validatorService: PepValidatorService);
    ngOnInit(): void;
    private isNumber;
    private isInteger;
    private isDecimal;
    private isPhone;
    private isText;
    onChange(e: any): void;
    onKeyDown(e: KeyboardEvent): void;
    validateValue(value: string): void;
}
