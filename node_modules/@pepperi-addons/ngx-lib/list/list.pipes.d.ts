import { PipeTransform } from '@angular/core';
import { ObjectsDataRow } from '@pepperi-addons/ngx-lib';
import { PepListSelectionType } from './list.model';
export declare class IsItemSelectedPipe implements PipeTransform {
    SEPARATOR: string;
    private getUniqItemId;
    transform(item: ObjectsDataRow, selectionTypeForActions: PepListSelectionType, selectedItemId: string, isAllSelected: boolean, selectedItems: Map<string, string>, unSelectedItems: Map<string, string>): any;
}
export declare class IsItemDisabledPipe implements PipeTransform {
    transform(item: ObjectsDataRow, lockItemInnerEvents: boolean, printMode: boolean): any;
}
