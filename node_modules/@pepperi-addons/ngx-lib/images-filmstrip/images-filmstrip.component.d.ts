import { OnInit, OnDestroy, AfterViewInit, ElementRef, TemplateRef, Renderer2, ChangeDetectorRef, OnChanges } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { MatDialogRef } from '@angular/material/dialog';
import { TranslateService } from '@ngx-translate/core';
import { Gallery, GalleryConfig, GalleryItem, GalleryRef } from 'ng-gallery';
import { PepCustomizationService, PepLayoutType, PepLayoutService, PepFileService, PepHorizontalAlignment } from '@pepperi-addons/ngx-lib';
import { PepDialogService } from '@pepperi-addons/ngx-lib/dialog';
import 'hammerjs';
interface IPepImagesFilmstripDialogData {
    currIndex: number;
    key: string;
    value: string;
    label: string;
    uid: string;
    showThumbnails: boolean;
}
import { ImageViewerConfig } from '@hallysonh/ngx-imageviewer';
export declare function createViewerConfig(translate: TranslateService): ImageViewerConfig;
export declare class PepImagesFilmstripComponent implements OnInit, OnChanges, AfterViewInit, OnDestroy {
    private customizationService;
    private dialogService;
    private layoutService;
    private fileService;
    private gallery;
    private renderer;
    private element;
    private cd;
    private dialogRef;
    private data;
    key: string;
    value: string;
    label: string;
    xAlignment: PepHorizontalAlignment;
    private _rowSpan;
    set rowSpan(value: number);
    get rowSpan(): number;
    controlType: string;
    form: FormGroup;
    uid: string;
    showTitle: boolean;
    private _layoutType;
    set layoutType(value: PepLayoutType);
    get layoutType(): PepLayoutType;
    currIndex: number;
    showThumbnails: boolean;
    galleryCont: ElementRef;
    galleryDialogTemplate: TemplateRef<any>;
    galleryDialogCont: ElementRef;
    config: GalleryConfig;
    items: GalleryItem[];
    inDialog: boolean;
    mandatory: boolean;
    readonly: boolean;
    disabled: boolean;
    fieldHeight: string;
    standAlone: boolean;
    galleryWidth: number;
    galleryHeight: any;
    isVertical: boolean;
    galleryRef: GalleryRef;
    dialogGalleryRef: GalleryRef;
    constructor(customizationService: PepCustomizationService, dialogService: PepDialogService, layoutService: PepLayoutService, fileService: PepFileService, gallery: Gallery, renderer: Renderer2, element: ElementRef, cd: ChangeDetectorRef, dialogRef: MatDialogRef<PepImagesFilmstripComponent>, data: IPepImagesFilmstripDialogData);
    private setFieldHeight;
    private setDefaultForm;
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnChanges(): void;
    ngOnDestroy(): void;
    initGalleryStyle(galleryContainer: any, galleryRef: any): void;
    onError(e: any): void;
    enlargeImage(event: any): void;
    afterDialogOpened(): void;
    setThumbnailDimension(galleryContainer: any): void;
    indexChange(item: any): void;
}
export {};
